#!/bin/bash

# Ensure we are in the project root
cd "$(dirname "$0")/.."

# Set PYTHONPATH to the project root so src is discoverable
export PYTHONPATH=$PYTHONPATH:$(pwd)

# Pass all arguments ($@) from the shell script to the python script
python3 src/main.py "$@"